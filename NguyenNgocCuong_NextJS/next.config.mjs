/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['127.0.0.1', 'localhost', 'images.unsplash.com'], // hỗ trợ cả local và ảnh mẫu từ Unsplash
  },
}

export default nextConfig;
